#pragma once

#include "HedgehogMathAPI.hpp"

namespace HM
{
    HEDGEHOG_MATH_API float ToRadians(float angle);
    HEDGEHOG_MATH_API double ToRadians(double angle);

    HEDGEHOG_MATH_API float ToDegree(float angle);
    HEDGEHOG_MATH_API double ToDegree(double angle);
}