# The game view (RENDERING.md section 7): what a camera entity renders, in the editor's game panel
# and in a game build. The shadow atlas is not the view's own: the shared frame phase renders it
# once for every view (SharedPhase.hpp), and the graph imports it.
# Held to a hand-written C++ twin by RenderGraphTest (test_shipped_graphs.cpp).
version: 2
outputs:
  - slot: 0
    name: color
    format: R16G16B16A16Unorm
    size: RelativeToResult(1.0)
resources:
  - name: depth
    format: D32Float
    size: RelativeToResult(1.0)
imports:
  - name: shadowAtlas
    format: D32Float
passes:
  - type: DepthPrepass
    name: DepthPrepass
    bindings:
      depth: depth
  - type: Forward
    name: Forward
    bindings:
      color: color
      depth: depth
      shadowMap: shadowAtlas
    parameters:
      cullBackFaces: true
