#pragma once

// Convenience aggregate header — include this to access the full RHI API.
// Concrete backends (e.g. VulkanDevice) are intentionally NOT included here;
// instantiate them only at the application entry point.

#include "IRHIDevice.hpp"
